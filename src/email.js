export function buildEmail(notices,firstRun=false){const subject=firstRun?`TU Notice Sentinel — Latest ${notices.length} Notices`:`TU Notice Sentinel — ${notices.length} New Notice${notices.length===1?"":"s"}`;const rows=notices.map((n,i)=>`<tr><td>${i+1}</td><td>${n.bsDate||"DATE?"}</td><td>${n.title}</td><td><a href="${n.url}">Open notice</a></td></tr>`).join("");return {subject,html:`<h2>🎓 TU Notice Sentinel</h2><p>${firstRun?"Initial notice digest":"New TU notices detected"}</p><table border="1" cellpadding="8"><tr><th>#</th><th>BS Date</th><th>Notice</th><th>Link</th></tr>${rows}</table>`,text:notices.map((n,i)=>`${i+1}. ${n.bsDate||"DATE?"} | ${n.title}\n${n.url}`).join("\n\n")};}
